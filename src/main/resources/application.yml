spring:
  application:
    name: algafood
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/algafood
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
    show-sql: true
